apply plugin: 'application'
apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
   compile project(':app')
  // compile project(':java-rest-api')
}
    
// mainClassName = project(':app').mainClassName
// mainClassName = project(':app').ext.mainClassName
mainClassName = "com.albertocolella.example_api.App"

project(':java-rest-api') {
	def appDir = project(':app').projectDir
    ext.customPropsFile = "$appDir/config.properties"
}